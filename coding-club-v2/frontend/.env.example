# ─── Coding Club Frontend — Environment Variables ────────────────────────────
# Copy this file to `.env` locally or set the var in your hosting dashboard.
# IMPORTANT: This value is baked into the bundle at BUILD time, so it must point
# to the public URL of your backend API (no trailing slash).
REACT_APP_API_URL=http://localhost:5000/api
